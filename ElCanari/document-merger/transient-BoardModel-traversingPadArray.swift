//--------------------------------------------------------------------------------------------------
//  THIS FILE IS REGENERATED BY EASY BINDINGS, ONLY MODIFY IT WITHIN USER ZONES
//--------------------------------------------------------------------------------------------------

import AppKit

//--------------------------------------------------------------------------------------------------

//--- START OF USER ZONE 1


//--- END OF USER ZONE 1

//--------------------------------------------------------------------------------------------------

@MainActor func transient_BoardModel_traversingPadArray (
       _ self_traversingPads_x : [BoardModelPad_x],      
       _ self_traversingPads_y : [BoardModelPad_y],      
       _ self_traversingPads_width : [BoardModelPad_width],
       _ self_traversingPads_height : [BoardModelPad_height],
       _ self_traversingPads_shape : [BoardModelPad_shape],
       _ self_traversingPads_rotation : [BoardModelPad_rotation]
) -> MergerPadArray {
//--- START OF USER ZONE 2
    var padArray = [MergerPad] ()
    var idx = 0
    while idx < self_traversingPads_x.count {
      let x = self_traversingPads_x [idx].x
      let y = self_traversingPads_y [idx].y
      let width = self_traversingPads_width [idx].width
      let height = self_traversingPads_height [idx].height
      let shape = self_traversingPads_shape [idx].shape
      let rotation = self_traversingPads_rotation [idx].rotation
      padArray.append (MergerPad (x:x, y:y, width:width, height:height, shape:shape, rotation:rotation))
      idx += 1
    }
    return MergerPadArray (padArray: padArray)
//--- END OF USER ZONE 2
}

//--------------------------------------------------------------------------------------------------
